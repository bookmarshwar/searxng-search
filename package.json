{
  "name": "searxng-search-skill",
  "private": true,
  "version": "0.2.0",
  "license": "MIT",
  "description": "Local SearXNG dual-instance search + URL fetch with 3-stage fallback (native fetch -> curl-via-proxy -> Jina Reader) and content-quality gating. Zero-external-runtime deps beyond bundled node_modules.",
  "dependencies": {
    "turndown": "^7.2.0"
  }
}